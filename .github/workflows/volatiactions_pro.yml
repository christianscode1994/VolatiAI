name: VolatiAI Pro Tier

on:
  schedule:
    - cron: "*/15 * * * *"   # every 15 minutes
  workflow_dispatch:

jobs:
  run-engine:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run VolatiAI engine (Pro)
        run: python -m src.main --tier pro

      - name: Copy output to docs
        run: |
          cp public/latest.json docs/latest_pro.json
          cp public/summary.html docs/summary_pro.html

      - name: Commit updated docs
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add docs/latest_pro.json docs/summary_pro.html
          git commit -m "Update Pro Tier data" || echo "No changes"

      - name: Push changes
        run: git push
