bot:
  name: "VolatiAI Slack Bot"
  enabled: true

targets:
  daily_channel: "#intel-daily"
  alerts_channel: "#intel-alerts"
  events_channel: "#intel-events"

format:
  blocks: true
  include_images: true

schedule:
  daily: "09:00"
  hourly: true
  alerts: true
